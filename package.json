{
  "name": "org-features",
  "version": "1.0.0",
  "description": "Dashboard tracking rec.us feature usage & adoption by organization",
  "scripts": {
    "render": "node scripts/ci-check-render.js",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node scripts/store.spec.js && node scripts/nav.spec.js && node scripts/bug-freshness.spec.js && node scripts/measured-features.spec.js && node scripts/org-features-settings.spec.js && node scripts/render-gate.spec.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@babel/parser": "^8.0.4",
    "puppeteer": "^24.43.1"
  }
}
